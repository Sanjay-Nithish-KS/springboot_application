pipeline {
    agent any 
    stages {
        stage('clean Package') {
            steps {
                sh 'ls -l'
                dir('/home/ubuntu/workspace/ultibranch_pipeline_releases_1.1/springboot_application') {
                    sh 'ls'
                }
            }
        }
    }
}
